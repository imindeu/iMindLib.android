<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@id/exo_video_player_dialog_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:keepScreenOn="true">

    <eu.imind.imindlib.videoplayer.media.VideoPlayerView
        android:id="@id/exo_video_player_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:surface_type="texture_view" />

    <TextView
        android:id="@id/exo_overlay_close_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="6dp"
        android:layout_marginEnd="6dp"
        android:layout_alignParentEnd="true"
        android:layout_alignTop="@id/exo_video_player_view"
        android:text="@string/close"
        android:textSize="14sp"
        android:textColor="@android:color/white"
        android:shadowColor="@android:color/black"
        android:shadowDx="1"
        android:shadowDy="1"
        android:shadowRadius="2"
        android:drawableEnd="@drawable/ic_arrow_down" />

</RelativeLayout>